#!/bin/bash

if [[ ! -d ~/.fzf ]]; then
  git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
else
  cd ~/.fzf
  git pull
fi

~/.fzf/install

cd ~
[[ ! -L .fzf.bash ]] && ln -s $SCRIPTDIR/fzf.bash .fzf.bash
[[ ! -L .fzf.zsh ]] && ln -s $SCRIPTDIR/fzf.zsh .fzf.zsh
