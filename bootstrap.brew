#!/bin/bash

[[ ! -x $(which brew) ]] && ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
brew update
brew upgrade
brew install bash-completion bazaar chruby ctags curl git go htop hub mercurial pt python ruby-install subversion the_silver_searcher thefuck vim wget neovim

pip install --upgrade pip
pip install --upgrade neovim
